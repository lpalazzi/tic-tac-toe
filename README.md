# Tic-Tac-Toe Web App

A simple tic-tac-toe game built with React ([source](https://reactjs.org/tutorial/tutorial.html))

## Run the app

In the project directory, run using the command ``npm start``.

Open [http://localhost:3000](http://localhost:3000) to view it in the browser.

